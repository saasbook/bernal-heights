%h1 All Hotspots

= render 'map'

%table#hotspots
  %thead
    %tr
      %th Issue Type
      %th Location
      %th Details
  %tbody
    - @hotspots.each do |hotspot|
      %tr
        %td= hotspot.issue_type
        %td= hotspot.location
        %td= hotspot.details

= link_to 'Create new hotspot', new_hotspot_path